# GREEN — image-compressor

## 驗證目標

- 多檔圖片輸入
- 品質控制
- 原始／輸出大小顯示
- 個別與批次下載
- 全程瀏覽器本機處理

## 驗證命令

```bash
node contract-test-image-compressor.cjs
```

## 狀態

實作已存在；本文件只記錄可重現命令。最終 PASS／FAIL 以目前工作樹重新執行的命令輸出與 Playwright 瀏覽器 Gate 為準，不沿用舊對話自述。
