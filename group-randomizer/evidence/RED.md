# RED — group-randomizer

實際 Chromium／`file://` 測試。

```bash
npx playwright test tests/starters.spec.js --config=playwright.config.js --grep group-randomizer
```

結果：5 tests；RED 4 passed / 1 failed；exit 1；複製後缺少可見完成回饋。
